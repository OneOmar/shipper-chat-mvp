# Frontend (Next.js) environment variables

# Backend base URL (used by Next.js rewrites to proxy /api/* + /socket.io/*)
BACKEND_URL=http://localhost:3001

# Optional: in local dev you can connect Socket.IO directly and bypass Next's proxy.
# Leave empty in production to use same-origin /socket.io.
NEXT_PUBLIC_SOCKET_SERVER_URL=

# Must match backend (frontend uses this to validate the auth cookie in server-rendered routes)
JWT_SECRET=change-me
AUTH_COOKIE_NAME=auth_token
