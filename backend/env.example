# Backend (Node.js + Express + Socket.IO) environment variables

PORT=3001

# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB?schema=public

# Auth (must match frontend)
JWT_SECRET=change-me
AUTH_COOKIE_NAME=auth_token

# Frontend origin(s) (CORS allow-list for Socket.IO in production)
# Comma-separated if needed.
FRONTEND_URL=http://localhost:3000

# Public origin for building absolute redirect URLs (Google OAuth, etc.)
# If omitted, the backend will attempt to infer from x-forwarded-* headers.
PUBLIC_APP_ORIGIN=http://localhost:3000

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# OpenAI (optional, used for AI chat)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# AI user (optional)
AI_USER_EMAIL=ai@local
